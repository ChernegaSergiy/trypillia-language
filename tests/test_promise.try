describe("Promise", fn() {
    it("basic resolve", fn() {
        let called = false;
        let exec = fn(resolve, reject) {
            called = true;
            resolve(42);
        };
        let p = Promise(exec);
        assertEq(called, true);
    });
});
